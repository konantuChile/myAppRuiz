<%= form_for(@tamanoempresa) do |f| %>
  <% if @tamanoempresa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tamanoempresa.errors.count, "error") %> prohibited this tamanoempresa from being saved:</h2>

      <ul>
      <% @tamanoempresa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tamanoempresa %><br>
    <%= f.text_area :tamanoempresa %>
  </div>
  <div class="field">
    <%= f.label :fechaingresotamnoemp %><br>
    <%= f.date_select :fechaingresotamnoemp %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
