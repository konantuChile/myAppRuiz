<%= form_for(@supervisor) do |f| %>
  <% if @supervisor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supervisor.errors.count, "error") %> prohibited this supervisor from being saved:</h2>

      <ul>
      <% @supervisor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :idempleado %><br>
    <%= f.number_field :idempleado %>
  </div>
  <div class="field">
    <%= f.label :fechaingresoencargado %><br>
    <%= f.date_select :fechaingresoencargado %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
